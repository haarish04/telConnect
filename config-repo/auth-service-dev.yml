server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/telconnect_dev
    username: root
    password: mydb
    driver-class-name: com.mysql.cj.jdbc.Driver

management:
  endpoint:
    gateway:
      enabled: true

  endpoints:
    web:
      exposure:
        include: "*"